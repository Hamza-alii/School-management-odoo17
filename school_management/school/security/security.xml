<odoo>
    <data noupdate="1">

        <!-- Tusaale: Koox cusub oo loogu talagalay Maamulka Dugsiga -->
        <record id="group_school_admin" model="res.groups">
            <field name="name">School Admin</field>
            <field name="category_id" ref="base.module_category_hidden"/>
        </record>

        <!-- Haddii aad rabto inaad isticmaasho rules, halkan ku dari kartaa -->
        <!--
        <record id="rule_student_own" model="ir.rule">
            <field name="name">Student: See Own</field>
            <field name="model_id" ref="model_school_student"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">['|', ('user_id','=',user.id), ('user_id','=',False)]</field>
        </record>
        -->

    </data>
</odoo>
